{
  "name": "ra-scan",
  "version": "1.1.2",
  "description": "Scan your ROM library and identify which games support RetroAchievements",
  "license": "GPL-3.0-only",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/TheDragonary/RetroAchievements-ROM-Scanner.git"
  },
  "homepage": "https://github.com/TheDragonary/RetroAchievements-ROM-Scanner",
  "bugs": {
    "url": "https://github.com/TheDragonary/RetroAchievements-ROM-Scanner/issues"
  },
  "type": "module",
  "bin": {
    "ra-scan": "dist/cli.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "start": "node dist/cli.js",
    "prepublishOnly": "npm run build",
    "build:linux": "npm run build && bun build dist/cli.js --compile --target=bun-linux-x64 --outfile ra-scan",
    "build:windows": "npm run build && bun build dist/cli.js --compile --target=bun-windows-x64 --outfile ra-scan",
    "build:macos": "npm run build && bun build dist/cli.js --compile --target=bun-darwin-x64 --outfile ra-scan",
    "build:linux-arm": "npm run build && bun build dist/cli.js --compile --target=bun-linux-arm64 --outfile ra-scan",
    "build:windows-arm": "npm run build && bun build dist/cli.js --compile --target=bun-windows-arm64 --outfile ra-scan",
    "build:macos-arm": "npm run build && bun build dist/cli.js --compile --target=bun-darwin-arm64 --outfile ra-scan"
  },
  "dependencies": {
    "commander": "^14.0.3",
    "dolphin-tool": "^0.2512.1",
    "env-paths": "^4.0.0"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "@types/node": "^25.5.0"
  },
  "engines": {
    "node": ">=20"
  },
  "keywords": [
    "retroarch",
    "retroachievements",
    "rom",
    "emulation",
    "scanner",
    "cli"
  ]
}
